name: Spell Check

on:
  pull_request:
    paths:
      - '**/*.md'  # Adjust file patterns as needed

jobs:
  spell-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Spell Check
        uses: yihui/actions-spellcheck@v1
