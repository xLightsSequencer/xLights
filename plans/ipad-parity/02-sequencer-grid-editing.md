# 02 · Sequencer Grid & Effect Editing

> The core single-/multi-effect editing surface is at strong parity. iPad's effect long-press menu, multi-select bulk menu, row-heading context menu, and timing-mark menu all share `src-core` ops with desktop and are real (not stubs). The two large desktop-only clusters are (1) the **entire AC/Auto-Color toolbar** (Off/On/Twinkle/Shimmer, Intensity, Ramp Up/Down/UpDown, Fill, Cascade, Foreground/Background, Select, Disabled) — zero references exist anywhere in `src-iPad`; **deferred by decision 2026-06-11** (all AC rows demoted to P3 — if demand surfaces, revisit as a *touch-first* design over the iPad's existing column span-select rather than a port of the desktop cell-cursor model); and (2) the **user-customizable keybinding system** (`KeyBindingEditDialog`, plus hotkey-to-apply-effect / -preset / -setting). Smaller desktop-only row-heading gaps: Play model, Convert-To-Effect (render-down to a single effect), Copy Effects incl SubModels, Copy Layers/SubModels to Models, Enable Render on All Models, Delete Multiple Layers, Paste-By-Cell/Paste-By-Time toggle, double-click-to-play-effect, and move-effect-up/down-a-layer via Up/Down arrows. **Cut is intentionally absent on iPad** (no menu item, no shortcut), and the iPad Up/Down arrows do **selection navigation** (Effect Above/Below), not layer-moving. iPad-only conveniences for this theme: two-finger marquee, Pencil-tap/squeeze→undo, pointer-hover handle highlights, long-press fade-handle → transition-type picker, and per-timing-mark Split/Merge/Add-Here touch ops. A third cluster (added #6268) is **Song Structure Regions** — its data model is wx-free core (`SongStructureManager`, round-tripped through `SequenceElements`/`SequenceFile`), and the iPad now has the core surface at parity: a translucent named-band overlay on the ruler+waveform strip, a long-press region menu (Edit Region name + 8-swatch color, Add/Delete Boundary, Views submenu), and "Create Song Regions from Timing Track" on the timing-row menu, all over a new `SongStructureManager` bridge. The region bulk ops (Copy-to-Region, Apply-Palette, Fill-from-Timing-Marks) and **per-region `.xsq` export** (audio-trimmed, via a wx-free `BuildDocument`-then-window-the-pugixml-tree path) are now bridged and wired into the region/effect menus, bringing Song Structure to full parity. Desktop also added drag-to-reorder of top-level rows (#6507) — now at parity on iPad via an `.onDrag`/`.onDrop` long-press-drag over the shared `MoveSequenceElement` — and an Effect Wheel radial keybinding quick-drop (#6486, see theme 04), the latter still iPad-missing. iPad additionally gained ⌘/Ctrl-tap + ⇧-tap additive multi-select and palette drag-create on empty cells, bringing both to parity. Desktop's Shift+double-click row-heading expand/collapse of strands (#6515) is a gesture over a shared-core strand-collapse op the iPad already reaches via its row context menu — parity by different idiom.

## Parity scorecard

| Feature | Surface | Desktop | iPad | Gap | Priority | Ease | Feasibility | Notes |
|---|---|---|---|---|---|---|---|---|
| Copy effect | context-menu | ✅ | ✅ | parity | P1 | easy | feasible | Desktop EffectsGrid.cpp:367; iPad SequencerGridV2View.swift:607 + ⌘C (XLightsCommands.swift:113). |
| Cut effect | context-menu/shortcut | ✅ | ✅ | parity | P2 | easy | feasible | Desktop EffectsGrid.cpp:366. iPad: Edit ▸ Cut (⌘X) + effect context-menu Cut → `cutSelectedEffects()` (copy + delete, one undo action). |
| Paste effect | context-menu | ✅ | ✅ | parity | P1 | easy | feasible | Desktop EffectsGrid.cpp:368 (paste mode-dependent). iPad "Paste Here" SequencerGridV2View.swift:609 + ⌘V at play head. |
| Delete effect | context-menu/key | ✅ | ✅ | parity | P1 | easy | feasible | Desktop EffectsGrid.cpp:369 / Delete key (MainSequencer.cpp:964). iPad SequencerGridV2View.swift:690 + Delete key. |
| Duplicate effect | shortcut | ✅ | ✅ | parity | P1 | easy | feasible | Desktop EffectsGrid.cpp:391 / ⌘D. iPad XLightsCommands.swift:127 → duplicateSelectedEffect (VM:5828). |
| Duplicate effect "Include Submodels" option (2026.10) | panel | ✅ | ✅ | parity | P2 | easy | feasible | Desktop `DuplicatePanel.cpp` checkbox `E_CHECKBOX_Duplicate_Include_Submodels`; render logic in `src-core/render/RenderEngine.cpp:285`. iPad: `resources/effectmetadata/Duplicate.json` already contains `Duplicate_Include_Submodels` checkbox; iPad `EffectMetadataPanel` renders it automatically. Core rendering is shared — parity is automatic. |
| Edit timing (start/end seconds) | dialog | ✅ | ✅ | parity | P1 | easy | feasible | Desktop "Timing" EffectsGrid.cpp:486 → EffectTimingDialog. iPad alert SequencerGridV2View.swift:632/765, live duration readout. |
| Edit effect description | dialog | ✅ | ✅ | parity | P2 | easy | feasible | Desktop EffectsGrid.cpp:449. iPad SequencerGridV2View.swift:670. Both single + multi. |
| Split effect at play marker | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop "Split" EffectsGrid.cpp:381. iPad SequencerGridV2View.swift:627 (splitSelectedEffectAtPlayMarker VM:4551). |
| Create Timing from selected effect(s) | menu | ✅ | ✅ | parity | P2 | medium | feasible | Desktop "Create Timing" effect menu EffectsGrid.cpp:386 + row "Create Timing From Effects". iPad in top-chrome menu SequencerGridV2View.swift:1307 (createTimingTrackFromEffects VM:4747), **not** in effect long-press menu. |
| Reset effect to defaults | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop EffectsGrid.cpp:448. iPad SequencerGridV2View.swift:664 (resetSelectedEffectsToDefaults VM:3212). |
| Lock / Unlock effect | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop EffectsGrid.cpp:450/451. iPad SequencerGridV2View.swift:649 + bulk:578. |
| Disable / Enable render on effect | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop EffectsGrid.cpp:452/453. iPad SequencerGridV2View.swift:655 + bulk:581. |
| Randomize effect settings | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop "Create Random Effects" EffectsGrid.cpp:443 (needs cell-range). iPad "Randomise Settings" SequencerGridV2View.swift:661 (existing effect). See also AC-fill-of-random below. |
| Find / Find Next / Find Prev / Replace All | menu+context | ✅ | ✅ | parity | P2 | medium | feasible | Desktop EffectsGrid.cpp:545-548. iPad ⌘F → FindReplaceSheet (findNext/replaceAllFindMatches). iPad has no separate Find-Next/Prev menu items but the sheet provides them. |
| Find Possible Source Effects (node row) | context-menu | ✅ | ✅ | parity | P3 | medium | feasible | Desktop EffectsGrid.cpp:476 (master-view, node rows). iPad: node-row context-menu "Find Possible Source Effects" (RowHeaderViews.swift `onFindSourceEffects`) → bridge `findSourceEffectsForRow:atMS:` (XLSequenceDocument.mm) traces the node's channel range (GetChannelForNode + ContainsChannel) back to model/strand/submodel effects active at the play head, returning each with a resolved visible (row, effectIndex). `SequencerViewModel.findSourceEffects` populates a results sheet (`SourceEffectsSheet.swift`); tapping a match seeks + selects it when its row is visible. |
| Multi-select via marquee | gesture | ✅ | ✅ | parity | P1 | easy | feasible | Desktop click-drag rect. iPad two-finger drag → onMarqueeSelect (EffectsMetalGridView.swift:58-61). |
| Select All effects (whole sequence) | menu/shortcut | ✅ | ✅ | parity | P1 | easy | feasible | Desktop ⌘A context-aware. iPad ⌘A XLightsCommands.swift:136 (selectAllEffects). |
| Clear selection (Escape) | shortcut | ✅ | ✅ | parity | P1 | easy | feasible | Desktop Escape. iPad Escape XLightsCommands.swift:145. |
| Select All in Row | context-menu | ✅ | ✅ | parity | P1 | easy | feasible | Desktop RowHeading.cpp:587. iPad SequencerGridV2View.swift:638 + RowHeaderViews.swift:523. |
| Select All in Model | context-menu | ✅ | ✅ | parity | P1 | easy | feasible | Desktop RowHeading.cpp:588. iPad SequencerGridV2View.swift:641 + RowHeaderViews.swift:532. |
| Select All in Column (time span) | context-menu | ❌ | ✅ | desktop-missing | P2 | easy | feasible | iPad-only SequencerGridV2View.swift:644 (selectAllEffectsInColumn VM:4401). Desktop's nearest is AC cell-range, not a true span-select across all rows. |
| Align Start / End / Both / Centers / Match Duration | context-menu | ✅ | ✅ | parity | P1 | easy | feasible | Desktop EffectsGrid.cpp:414-418. iPad SequencerGridV2View.swift:546-559. Multi-select. |
| Shift-Align Start / End | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop EffectsGrid.cpp:419/420. iPad SequencerGridV2View.swift:561/564. |
| Align to Closest Timing Mark | context-menu | ✅ | ✅ | parity | P1 | medium | feasible | Desktop EffectsGrid.cpp:421. iPad SequencerGridV2View.swift:567 (alignSelectedEffectsToTimingMarks VM:5081). |
| Close Gap between selected | context-menu | ✅ | ✅ | parity | P2 | medium | feasible | Desktop EffectsGrid.cpp:422. iPad SequencerGridV2View.swift:571 (closeGapInSelectedEffects VM:5133), gated on canCloseGapInSelection. |
| Save current as Effect Preset | context-menu | ✅ | ✅ | parity | P1 | medium | feasible | Desktop "Effect Presets" EffectsGrid.cpp:442 → EffectTreeDialog. iPad "Save as Preset…" SequencerGridV2View.swift:678 (PRE-1 on-disk library). |
| Apply Effect Preset | context-menu | ✅ | ✅ | parity | P1 | medium | feasible | Desktop submenu. iPad dynamic preset buttons SequencerGridV2View.swift:682 (applyPreset VM:3099) + Manage Presets → PresetBrowserSheet. |
| AC: Disabled toggle | toolbar | ✅ | ❌ | ipad-missing | P3 | easy | hard | Desktop xLightsMain.cpp:852. Whole AC toolbar absent on iPad (no AC refs in src-iPad). |
| AC: Select mode | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:854 (Shift+L). Needs cell-range grid model iPad lacks. |
| AC: Off effect | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:855 (Delete). HandleACKey EffectsGrid.cpp. |
| AC: On effect | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:856 (O). |
| AC: Shimmer effect | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:857 (S). |
| AC: Twinkle effect | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:858 (K). |
| AC: Intensity style | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:860 (I). |
| AC: Ramp Up style | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:861 (U). |
| AC: Ramp Down style | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:862 (D). |
| AC: Ramp Up/Down style | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop xLightsMain.cpp:863 (A). |
| AC: Parm1 / Parm2 choices | toolbar | ✅ | ❌ | ipad-missing | P3 | medium | hard | Desktop ChoiceParm1/2 xLightsMain.cpp:820/836 (style intensity/level args). |
| AC: Fill tool | toolbar | ✅ | ❌ | ipad-missing | P3 | hard | hard | Desktop xLightsMain.cpp:867 (F). Cell-range fill; biggest AC build. |
| AC: Cascade tool | toolbar | ✅ | ❌ | ipad-missing | P3 | hard | hard | Desktop xLightsMain.cpp:868 (H). |
| AC: Foreground mode | toolbar | ✅ | ❌ | ipad-missing | P3 | easy | hard | Desktop xLightsMain.cpp:870 (G). |
| AC: Background mode | toolbar | ✅ | ❌ | ipad-missing | P3 | easy | hard | Desktop xLightsMain.cpp:871 (B). |
| Paste By Cell mode toggle | toolbar | ✅ | ✅ | parity | P2 | medium | feasible | Desktop EditToolBar xLightsMain.cpp:816. iPad: top-chrome options menu "Paste Mode" picker (By Time / By Cell, `pasteByCell` @AppStorage, default By Cell preserving the B14 auto behavior) gating the cell branch in `pasteEffect`. |
| Paste By Time mode toggle | toolbar | ✅ | ✅ | parity | P2 | easy | feasible | Desktop xLightsMain.cpp:815 (default). iPad: "By Time" option of the Paste Mode picker (forces duration-preserving paste). |
| Nudge effect 1 ms (Ctrl+←/→) | shortcut | ✅ | ✅ | parity | P1 | easy | feasible | Desktop MoveSelectedEffectLeft/Right(…,control,…). iPad XLightsCommands.swift:172-181 (nudgeSelectedEffect ±1). |
| Nudge effect 1 frame (Opt+←/→) | shortcut | ✅ | ✅ | parity | P1 | easy | feasible | Desktop alt-arrow. iPad XLightsCommands.swift:186-195 (by frameIntervalMS). |
| Stretch effect end (Shift+←/→) | shortcut | ✅ | ✅ | parity | P1 | easy | feasible | Desktop MoveSelectedEffect*(shift,…). iPad XLightsCommands.swift:158-167 (stretchSelectedEffectEnd). |
| Extend to previous / next effect (Shift+⌘+←/→) | shortcut | ✅ | ✅ | parity | P2 | easy | feasible | Desktop butt-up (Ctrl+Shift). iPad XLightsCommands.swift:203-212 (extendSelectedEffectTo*). |
| Nudge to prev / next timing mark (Ctrl+PgUp/PgDn) | shortcut | ✅ | ✅ | parity | P2 | medium | feasible | Desktop Ctrl-arrow butt to timing. iPad XLightsCommands.swift:221-231 (nudgeSelectedEffectToTimingMark). |
| Move effect up/down one layer (⌥↑/⌥↓) | shortcut | ✅ | ✅ | parity | P2 | medium | feasible | Desktop MoveSelectedEffectUp/Down (MainSequencer.cpp:986-992). iPad: `moveSelectedEffectToAdjacentLayer` over `moveEffectToRow` (within-element layer adjacency, collision-safe), menu + ⌥↑/⌥↓ (plain arrows stay selection nav). |
| Drag effect to another row/layer | gesture | ✅ | ✅ | parity | P1 | medium | feasible | Desktop drag. iPad onMoveEffectToRow (SequencerGridV2View.swift:1820). |
| Drag effect to move / resize edges | gesture | ✅ | ✅ | parity | P1 | medium | feasible | Desktop edge/center drag. iPad onMoveEffect/onResizeEdge (SequencerGridV2View.swift:1816/1825). |
| Drag shared edge between two adjacent effects | gesture | ✅ | ✅ | parity | P2 | medium | feasible | Desktop shared-boundary drag. iPad onResizeSharedEdge (SequencerGridV2View.swift:1829, resizeSharedEdge). |
| Drag fade-in / fade-out handle (time) | gesture | ✅ | ✅ | parity | P1 | medium | feasible | Desktop fade handle. iPad fadeIn/fadeOut handle kinds (EffectsMetalGridView.swift:177) → onAdjustFade. |
| Pick transition TYPE from fade handle | context-menu/gesture | 🟡 | ✅ | desktop-missing | P3 | medium | feasible | iPad long-press fade handle → transition picker (SequencerGridV2View.swift:83-118, onRequestTransitionMenu:1848). Desktop sets type only via Blending panel, not the grid. |
| Create effect: select palette + tap grid | gesture | 🟡 | ✅ | parity | P1 | hard | hard | Desktop drag-from-toolbar (EffectDropTarget). iPad tap palette (EffectPaletteView.swift:32 selectPaletteEffect) then tap empty cell (addEffectFromPaletteTap VM:5334). Different paradigm; both achieve placement. |
| Random effect button (palette) | panel | ✅ | ✅ | parity | P2 | easy | feasible | Desktop Random toolbar button. iPad EffectPaletteView.swift:14 (availableEffects.randomElement). |
| Double-click effect to play | gesture | ✅ | ✅ | parity | P2 | medium | feasible | Desktop mouseLeftDClick → RaisePlayModelEffect (EffectsGrid.cpp:259/7600). iPad idiom: "Play Effect" in the effect context menu → `playSelectedEffect()` loop-plays the effect's range via the loop-region plumbing. |
| Undo / Redo (with action name) | menu/shortcut | ✅ | ✅ | parity | P1 | easy | feasible | Desktop ⌘Z/⌘⇧Z + named in context menu. iPad XLightsCommands.swift:68-87 (undoMenuTitle). |
| User-customizable keybindings (editor) | preference/dialog | ✅ | ❌ | ipad-missing | P3 | hard | hard | Desktop KeyBindingEditDialog + KeyBindings system. iPad shortcuts are fixed in XLightsCommands.swift; no editor. |
| Apply effect by hotkey | shortcut | ✅ | ❌ | ipad-missing | P3 | medium | feasible | Desktop keybinding type "EFFECT"/"PRESET"/"APPLYSETTING" (MainSequencer.cpp:628-664). iPad has no hotkey-to-apply. |
| Insert Layer Above / Below | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:486/487. iPad RowHeaderViews.swift:627/632. |
| Insert Multiple Layers Below | dialog | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:488. iPad RowHeaderViews.swift:638 → InsertLayersAlert. |
| Delete Layer | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:491. iPad RowHeaderViews.swift:644 (confirm). |
| Delete Multiple Layers | context-menu | ✅ | ✅ | parity | P3 | easy | feasible | Desktop RowHeading.cpp:492. iPad: "Delete Layers…" count prompt (`DeleteLayersAlert`) → `deleteLayersBelow(rowIndex:count:)`, clamped to keep the element's last layer. |
| Delete Unused Layers | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:506. iPad RowHeaderViews.swift:650 (count shown). |
| Edit Layer Name | context-menu | ✅ | ✅ | parity | P3 | easy | feasible | Desktop "Edit Layer Name" RowHeading.cpp:508. iPad "Rename Layer" RowHeaderViews.swift:622. |
| Collapse/expand element layers (+/-) | panel | ✅ | ✅ | parity | P2 | easy | feasible | Desktop +/- + double-click. iPad +/- RowHeaderViews.swift:430 (rowIsElementCollapsed). |
| Shift+double-click row → expand/collapse strands | gesture | ✅ | ✅ | parity | P3 | easy | feasible | Desktop #6515 (RowHeading.cpp Shift+dclick; core Element/SequenceElements strand-collapse op shared). iPad toggles strands via row context menu "Toggle Strands" (RowHeaderViews.swift:658) — same op, touch idiom. |
| Collapse All / Expand All (Models & Layers) | menu | ✅ | 🟡 | ipad-weaker | P2 | easy | feasible | Desktop has separate "Collapse All Models" and "Collapse All Layers" (RowHeading.cpp:536/537). iPad top-chrome "Collapse All"/"Expand All" (SequencerGridV2View.swift:1335-1343) is a single collapseAllElements via SetCollapsed only (XLSequenceDocument.mm:980-990), without the separate model-vs-layer distinction. |
| Show All Effects | menu | ✅ | ✅ | parity | P3 | easy | feasible | Desktop RowHeading.cpp:535. iPad SequencerGridV2View.swift:1345. |
| Show/Hide Strands/Submodels on row | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop "Toggle Strands/Models" RowHeading.cpp:528/533. iPad RowHeaderViews.swift:658. |
| Show/Hide Nodes on strand row | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop "Toggle Nodes" RowHeading.cpp:530. iPad RowHeaderViews.swift:675. |
| Show All / Hide Unused Submodels | context-menu | ✅ | ✅ | parity | P3 | easy | feasible | Desktop RowHeading.cpp:515/517. iPad RowHeaderViews.swift:665. |
| Submodel/node expand chevron | panel | ✅ | ✅ | parity | P3 | easy | feasible | Desktop chevron. iPad RowHeaderViews.swift:445. |
| Cut / Copy Row effects | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:589/591. iPad RowHeaderViews.swift:548/553 (copyRow/cutRow VM:4936/4942). |
| Cut / Copy Model effects (all layers) | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:590/592. iPad RowHeaderViews.swift:558/563 (copyModel VM:4949). |
| Copy Effects incl SubModels | context-menu | ✅ | ✅ | parity | P3 | medium | feasible | Desktop RowHeading.cpp:593 (ID_ROW_MNU_COPY_MODEL_INCL_SUBMODELS). iPad: row-menu "Copy Effects incl SubModels" (RowHeaderViews.swift `onCopyModelInclSubmodels`) → bridge `copyModelInclSubmodelsAtRow:` (XLSequenceDocument.mm) walks the ModelElement's own layers then every submodel's layers directly (not just visible rows, so collapsed submodels are captured), and `SequencerViewModel.copyModelInclSubmodels` re-anchors them into the multi-row `ClipboardEntry` format. `pasteAtRow` reconstructs across rows as for plain Copy Model. |
| Copy Layers/SubModels to Models | context-menu | ✅ | ✅ | parity | P3 | hard | feasible | Desktop RowHeading.cpp:594 (multi-model fan-out copy, incl. submodel layers). iPad: "Copy Layers/SubModels to Models…" context-menu item (`RowHeaderViews.swift`, `onCopyLayersToModels`) → `CopyLayersToModelsSheet` model-picker → `SequencerViewModel.copyLayersSubmodelsToModels` (`SequencerViewModel.swift`) fans out `copyModel` + `pasteAtRow` to each target with undo grouping. |
| Paste at Row / Model | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:595/596. iPad "Paste" RowHeaderViews.swift:569 (pasteAtRow VM:5991, auto-inserts layers). |
| Reorder top-level rows (drag model/group/timing track) | gesture | ✅ | ✅ | parity | P2 | medium | feasible | Desktop drag-to-reorder in row heading (#6507): RowHeading.cpp mouseMove/mouseLeftUp (`_rowDragging` → `SequenceElements::MoveSequenceElement`) + RowHeading.h drag state. Core `MoveSequenceElement` op is wx-free/shared. iPad now drag-reorders top-level rows: `.onDrag`/`.onDrop` long-press-drag with an accent drop indicator (`TopLevelRowReorderModifier` in RowHeaderViews.swift; wired in SequencerGridV2View.swift `modelHeaders`), the `moveTopLevelRow(from:toBefore:)` + name-based undo bridge methods on XLSequenceDocument (`.mm` calls the shared `MoveSequenceElement`), and a `SequencerViewModel.moveTopLevelRow` wrapper that registers an exact-order undo (snapshots the source element's original successor) and reloads rows. Non-top-level rows (layers/strands/nodes/submodels) stay undraggable, matching desktop's block-drag. |
| Paste selected effects at original time | context-menu/shortcut | ✅ | ✅ | parity | P2 | medium | feasible | Desktop #5064: paste copied effects back at their original absolute timeline positions (distinct from the by-cell/by-time toggle). iPad: copy now captures the anchor's absolute (row, ms) in `ClipboardEntry` (`anchorAbsRow`/`anchorAbsStartMS`); `SequencerViewModel.pasteAtOriginalTime()` restores each entry to `anchorAbsRow + rowOffset` / `anchorAbsStartMS + startOffsetMS` with the same per-effect clear-range overlap handling `pasteEffect` uses. Surfaced as Edit ▸ "Paste at Original Time" (⌥⌘V, XLightsCommands.swift) + the effect context menu (SequencerGridV2View.swift), gated on `clipboardHasOriginalTime`. |
| Delete all effects on Row | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:601. iPad RowHeaderViews.swift:540 (confirm). |
| Delete Model / SubModel / Strand / Node effects | context-menu | ✅ | ✅ | parity | P3 | easy | feasible | Desktop RowHeading.cpp:603-606. iPad: "Delete Model Effects" (element-wide incl. submodels/strands/nodes, `deleteModelEffects`) + "Delete All Effects on Row" invoked from any submodel/strand/node row covers the scoped variants by idiom. |
| Convert Effects to 'Per Model' (model & layer scope) | context-menu | ✅ | ✅ | parity | P2 | hard | feasible | Desktop RowHeading.cpp:609/610. iPad RowHeaderViews.swift:590/595 (convertEffectsToPerModel VM:2954). |
| Convert To Effect (render-down to single effect) | context-menu | ✅ | ❌ | ipad-missing | P3 | hard | feasible | Desktop RowHeading.cpp:541/548/552 (ID_ROW_MNU_CONVERT_TO_EFFECTS). No iPad bridge. |
| Promote Node Effects | context-menu | ✅ | ✅ | parity | P2 | hard | feasible | Desktop RowHeading.cpp:557. iPad RowHeaderViews.swift:605 (promoteNodeEffects VM:2970). |
| Disable/Enable render on Model | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:565/567. iPad RowHeaderViews.swift:611. |
| Enable Render on All Models | context-menu | ✅ | ✅ | parity | P3 | easy | feasible | Desktop RowHeading.cpp:579. iPad: row context-menu item → `enableRenderOnAllModels()` clearing the element render-disable flag everywhere. |
| Play Model (row heading) | context-menu | ✅ | ✅ | parity | P2 | medium | feasible | Desktop RowHeading.cpp:582 (ID_ROW_MNU_PLAY_MODEL). iPad: row context-menu "Play Model" → `playModel(rowIndex:)` loop-plays the element's effects span (contiguous layer rows). |
| Export Model as FSEQ (+ loop-range) | context-menu | ✅ | 🟡 | ipad-weaker | P2 | medium | feasible | Desktop RowHeading.cpp:583/584 + selected-effects variants. iPad 'Export Model as FSEQ…' / '(Loop Range)…' (RowHeaderViews.swift:573-583) wraps exportModelAsFSEQ(atRow:toPath:startMS:endMS:); covers raw/rendered FSEQ but not every desktop variant. |
| Export Selected Model Effects (FSEQ) | context-menu | ✅ | 🟡 | ipad-missing | P3 | medium | feasible | Desktop RowHeading.cpp:585/586. iPad exports whole model / loop range, not a selected-effects-only FSEQ. |
| Rename Timing Track | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:621. iPad RowHeaderViews.swift:159. |
| Delete Timing Track | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop RowHeading.cpp:622. iPad RowHeaderViews.swift:266. |
| Make Timing Track Variable (unfix) | context-menu | ✅ | ✅ | parity | P3 | easy | feasible | Desktop RowHeading.cpp:625. iPad "Make Variable" RowHeaderViews.swift:198. |
| Breakdown Phrases (timing track) | context-menu | ✅ | ✅ | parity | P1 | hard | feasible | Desktop RowHeading.cpp:644 + effect menu EffectsGrid.cpp:504. iPad RowHeaderViews.swift:164. |
| Breakdown Words (timing track) | context-menu | ✅ | ✅ | parity | P1 | hard | feasible | Desktop RowHeading.cpp:646 + EffectsGrid.cpp:509. iPad RowHeaderViews.swift:170. |
| Breakdown This Phrase (per-mark) | context-menu | ✅ | ✅ | parity | P2 | hard | feasible | Desktop per-phrase EffectsGrid.cpp:504. iPad per-mark SequencerGridV2View.swift:745 (canBreakdownPhrase). |
| Remove Words / Phonemes / Both | context-menu | ✅ | ✅ | parity | P3 | medium | feasible | Desktop RowHeading.cpp:649-656. iPad RowHeaderViews.swift:177-193. |
| Halve Timing Marks | context-menu | ✅ | ✅ | parity | P2 | medium | feasible | Desktop "Divide Timings" EffectsGrid.cpp:514. iPad "Halve Timing Marks" RowHeaderViews.swift:244. |
| Divide Timing Marks by N (2/3/4/6/8) | context-menu | ✅ | ✅ | parity | P2 | medium | feasible | Desktop DivideTimingTrack. iPad RowHeaderViews.swift:250-255. |
| Generate Subdivided Timing Track | context-menu | ✅ | ✅ | parity | P3 | medium | feasible | Desktop RowHeading.cpp:631. iPad RowHeaderViews.swift:204-213. |
| Auto-Label Timing Marks | dialog | ✅ | ✅ | parity | P2 | easy | feasible | Desktop "Auto Label Timings" EffectsGrid.cpp:516. iPad RowHeaderViews.swift:238 / alert SequencerGridV2View.swift:800. |
| Select Timing Marks (whole track) | context-menu | ✅ | ✅ | parity | P3 | easy | feasible | Desktop RowHeading.cpp:630. iPad "Select All Marks" RowHeaderViews.swift:262. |
| Add/Remove "-shimmer" suffix (timing mark) | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop EffectsGrid.cpp:519/520. iPad SequencerGridV2View.swift:721 (toggleShimmerSuffixOnMark). |
| Split timing mark at play marker (per-mark) | context-menu | 🟡 | ✅ | desktop-missing | P3 | medium | feasible | Desktop has TIMING_SPLIT keybinding (SplitTimingMark) but no per-mark right-click item. iPad SequencerGridV2View.swift:728 (splitTimingMark VM:3634). |
| Merge timing mark with next (per-mark) | context-menu | ❌ | ✅ | desktop-missing | P3 | medium | feasible | iPad-only SequencerGridV2View.swift:736 (mergeTimingMarkWithNext VM:3662). Desktop merges by deleting a boundary, no explicit op. |
| Add timing mark by tap / click | gesture | ✅ | ✅ | parity | P2 | easy | feasible | Desktop click on selected timing row / TIMING_ADD. iPad "Add Mark Here" SequencerGridV2View.swift:756. |
| Delete timing mark | context-menu | ✅ | ✅ | parity | P2 | easy | feasible | Desktop delete. iPad SequencerGridV2View.swift:750 (deleteTimingMark). |
| Two-finger marquee (touch idiom) | gesture | ❌ | ✅ | desktop-missing | P3 | easy | infeasible | iPad-only; desktop uses single-button drag rect. Touch idiom, no desktop need. |
| Pencil double-tap / squeeze → undo | gesture | ❌ | ✅ | desktop-missing | P3 | easy | infeasible | iPad-only UIPencilInteraction (EffectsMetalGridView.swift:334-344). No desktop hardware. |
| Pointer-hover handle highlight (Magic Keyboard) | gesture | 🟡 | ✅ | parity | P3 | easy | infeasible | iPad B30 hover (EffectsMetalGridView.swift:173). Desktop has mouse-move cursor feedback; equivalent idiom. |
| Drag-create effect by dragging empty cell range | gesture | ✅ | ✅ | parity | P1 | medium | feasible | Desktop drags out a new effect across an empty cell range. iPad now drag-creates: with a palette effect armed, a horizontal pan over empty model-row space sizes a new effect (live translucent preview + time pill, snaps to timing marks honouring `snapToTimingMarks`). `EffectsMetalGridView` `beginCreateDrag`/`updateCreateDrag`/`endCreateDrag` → `onCreateEffectDrag` → `SequencerViewModel.createEffectFromPaletteDrag` (reuses the palette-tap creation/clamp/select path with an explicit start/end). |
| Ctrl-click / Shift-click additive multi-select | gesture | ✅ | ✅ | parity | P2 | medium | feasible | Desktop adds to/extends selection with modifier-clicks. iPad now mirrors it: the tap handler reads `UIGestureRecognizer.modifierFlags` — ⌘/Ctrl-tap toggles the effect in/out of the set (`onToggleSelectEffect` → `toggleSelectEffect`), ⇧-tap extends from the anchor across the spanned rows/time range (`onExtendSelectEffect` → `extendSelectEffect`). Marquee + bulk menus remain for touch-only use. |
| Paste As Layers mode (+ Alternate Paste) | context-menu | ✅ | 🟡 | ipad-weaker | P3 | medium | feasible | iPad pasteAtRow auto-inserts layers (SequencerViewModel.swift:5991) but has no explicit Paste-As-Layers / Alternate-Paste mode (no PasteAsLayers/ALTERNATE_PASTE/alternatePaste refs in src-iPad). |
| Undo step memory cap (low-memory hosts) | shared core | ✅ | ✅ | parity | P3 | easy | feasible | Shared `src-core` UndoManager: SetMaxSteps (UndoManager.h:152), EnforceMaxSteps (:159); iPad sets the cap via iPadRenderContext.cpp:77 (SetMaxSteps). |
| Song Structure: create / name / color regions | timeline long-press (boundary + edit sheet) | ✅ | ✅ | parity | P2 | hard | feasible | Desktop #6268: boundary add/delete/drag + SongRegionEditDialog (TimeLine.cpp, 8-swatch picker). iPad: bridge over `SongStructureManager` (XLSequenceDocument.mm `songStructureRegions`/`addSongStructureBoundary`/`deleteSongStructureBoundary`/`moveSongStructureBoundary`/`setSongStructureRegionName`/`setSongStructureRegionColor`); translucent named bands drawn on the ruler+waveform strip (TopChromeMetalGridView.swift draw); long-press band → "Edit Region…" (SongRegionEditSheet.swift name + 8 palette swatches) / "Add Boundary Here" / "Delete Boundary" (SequencerGridV2View.swift). Region edits are non-undoable, matching desktop. |
| Song Structure: named views (switch / new / duplicate / rename / delete) | timeline long-press submenu | ✅ | ✅ | parity | P3 | medium | feasible | Desktop Views submenu (TimeLine.cpp). iPad: same submenu in the region long-press dialog (SequencerGridV2View.swift) over bridge `songStructureViewNames`/`setSongStructureActiveView`/`addSongStructureView`/`duplicateSongStructureView`/`renameSongStructureView`/`deleteSongStructureView` → SongStructureManager AddView/DuplicateView/RenameView/DeleteView/SetActiveView. |
| Song Structure: Create from Timing Track | row-heading menu | ✅ | ✅ | parity | P2 | medium | feasible | Desktop "Create Song Regions from Timing Track" (RowHeading.cpp:1831). iPad: timing-row context menu item (RowHeaderViews.swift `onCreateSongRegions`) → bridge `createSongRegionsFromTimingRow` reproduces the desktop loop (one region per mark + gap fillers, palette-cycled colors). |
| Song Structure: region bulk actions (Copy Effects to Region / Apply Palette / Fill from Timing Marks) | timeline + grid menus | ✅ | ✅ | parity | P2 | hard | feasible | Desktop TimeLine.cpp:1774/1845 + EffectsGrid.cpp:8861. iPad: wx-free bridge ops `copyEffects(fromRegion:toRegion:)`, `applyPalette(_:toRegionAtIndex:)`, `fillRegionFromTimingMarks(sourceRow:sourceIndex:)` (XLSequenceDocument.mm — single undo step, clear-range checks, frame snapping faithful to desktop) wired into the region long-press menu (Copy-to-Region target submenu, Apply-Selected-Effect-Palette) and the effect context menu (Fill Region from Timing Marks) in SequencerGridV2View.swift. |
| Song Structure: export region(s) as new .xsq (audio trimmed) | timeline right-click | ✅ | ✅ | parity | P3 | hard | feasible | Desktop DoExportSongRegion + Export-All (TabSequence.cpp:2114/2342). iPad: bridge `exportSongRegionToPath:startMS:endMS:name:` (XLSequenceDocument.mm). Rather than deep-copy `SequenceElements` (no copy-ctor; needs a live RenderContext), it calls the wx-free `SequenceFile::BuildDocument(pugi::xml_document&, SequenceElements&)` — the iPad's own save path — to emit the full document, then **windows the pugixml tree in place**: every `<Effect>` node under `ElementEffects` with `endTime ≤ startMS` or `startTime ≥ endMS` is dropped, survivors are clipped to the region and offset by `-startMS` (faithful to desktop `WriteRegionEffects`); `head/sequenceDuration` is trimmed, `head/song` set to the region name, and `head/mediaFile` repointed to a trimmed `.m4a` sibling produced via `AudioManager::EncodeAudio` over `GetRawLeft/RightDataPtr` (same sample math as desktop). UI: region long-press "Export Region as Sequence…" (SequencerGridV2View.swift) → `.fileImporter(.folder)` destination pick (so the `.xsq` + sibling `.m4a` land together, audio reference intact) → `SequencerViewModel.exportSongRegion(_:toFolder:)`. Export-All is not yet surfaced (single-region only). |

## iPad gaps (desktop has, iPad missing)

### Deferred (decision 2026-06-11)
- **AC / Auto-Color toolbar (entire cluster)** — Select / Off / On / Shimmer / Twinkle / Intensity / Ramp Up / Ramp Down / Ramp Up-Down / Fill / Cascade + Parm1/Parm2 + Foreground/Background + Disabled. Desktop builds it in `xLightsMain.cpp:819-873` and drives it through `EffectsGrid::HandleACKey` / `DoACDraw` / `SetACSettings` (EffectsGrid.cpp:3290-3326). **Zero references anywhere in `src-iPad`.** AC works on a *cell-range* selection model (rows × timing columns) that the iPad grid does not implement — it tracks discrete selected effects, not a row/column cursor with `mRangeStartCol/mRangeEndCol`. **Deferred:** demoted to P3 on 2026-06-11 — a moded toolbar + single-letter-key workflow is a mouse/keyboard idiom of uncertain touch value. If user demand surfaces, scope it as a *touch-first* redesign that applies the core AC render ops (which are wx-free and reusable) to the iPad's existing selection idioms (marquee, `selectAllEffectsInColumn` span-select) via a contextual action bar — not as a port of the desktop cell-cursor.

### P1
- ~~**Drag-create effect by dragging an empty cell range**~~ — **landed**: with a palette effect armed, a horizontal pan over empty model-row space in `EffectsMetalGridView` drags out a new effect (`beginCreateDrag`/`updateCreateDrag`/`endCreateDrag`) with a live translucent preview + time pill and timing-mark snapping (honours `snapToTimingMarks`). Commits through `onCreateEffectDrag` → `SequencerViewModel.createEffectFromPaletteDrag`, reusing the palette-tap creation/clamp/select-on-create path with an explicit start/end.

### P2
- ~~**Move effect up/down a layer**~~ — **landed**: `moveSelectedEffectToAdjacentLayer` + menu items + ⌥↑/⌥↓ (plain arrows stay selection nav).
- ~~**Play Model from row heading** / **Play Effect**~~ — **landed** as loop-region idioms: row menu "Play Model" (`playModel(rowIndex:)`, element span) and effect menu "Play Effect" (`playSelectedEffect()`).
- ~~**Paste By Cell / Paste By Time toggle**~~ — **landed**: top-chrome "Paste Mode" picker (`pasteByCell` @AppStorage, default By Cell) gating the B14 cell branch.
- ~~**Cut effect**~~ — **landed**: Edit ▸ Cut (⌘X) + context-menu Cut via `cutSelectedEffects()` (copy + delete, one undo action).
- ~~**Ctrl-click / Shift-click additive multi-select**~~ — **landed**: the `EffectsMetalGridView` tap handler reads `UIGestureRecognizer.modifierFlags` — ⌘/Ctrl-tap toggles an effect in/out of the selection (`onToggleSelectEffect` → `SequencerViewModel.toggleSelectEffect`), ⇧-tap extends from the anchor across the spanned rows + time range (`onExtendSelectEffect` → `extendSelectEffect`). Both route through `setMultiSelection`. Marquee + bulk menus stay for touch-only use. (Modifier-tap requires a Magic Keyboard/trackpad, the only way to surface modifier flags through a touch gesture.)
- ~~**Reorder top-level sequencer rows (drag model/group/timing track)**~~ — **landed** (#6507 parity): top-level model/group/timing-track headers carry an `.onDrag`/`.onDrop` long-press-drag (`TopLevelRowReorderModifier` in `RowHeaderViews.swift`, wired in `SequencerGridV2View.modelHeaders`) with an accent drop indicator. The bridge adds `moveTopLevelRow(from:toBefore:)` plus name-based undo helpers (`moveTopLevelElement(named:beforeNamed:)`, `topLevelElementName(after:)`) that call the shared wx-free `SequenceElements::MoveSequenceElement`. `SequencerViewModel.moveTopLevelRow` registers an exact-order undo (snapshots the source element's original successor) and reloads rows. Non-top-level rows (layers/strands/nodes/submodels) stay undraggable, matching desktop's block-drag.
- ~~**Paste selected effects at original time** (#5064)~~ — **landed**: copy captures the anchor's absolute (row, ms) in `ClipboardEntry`; `pasteAtOriginalTime()` restores absolute placement with `pasteEffect`'s clear-range overlap handling, surfaced as Edit ▸ "Paste at Original Time" (⌥⌘V) + the effect context menu.
- ~~**Song Structure Regions (full remainder)**~~ — **landed**: the three bulk effect ops (Copy-Effects-to-Region, Apply-Selected-Effect-Palette, Fill-Region-from-Timing-Marks) and **per-region `.xsq` export** are now at parity. Export reuses the wx-free `SequenceFile::BuildDocument` then windows the pugixml tree (drop/clip/offset effects to `[startMS,endMS)`), trims audio to a sibling `.m4a` via `AudioManager::EncodeAudio`, and lands `.xsq`+`.m4a` together via a `.fileImporter(.folder)` destination pick. Export-**All** regions (one file each) is the only sub-item not yet surfaced.

### P3
- **Delete scoped effects: SubModel / Strand / Node** (RowHeading.cpp:603-606), **Convert To Effect** render-down (RowHeading.cpp:541/548/552), **Export Selected Model Effects FSEQ** (RowHeading.cpp:585/586). Each is a thin menu/bridge addition over existing core ops; low frequency. (Copy Layers/SubModels to Models, **Copy Effects incl SubModels**, and **Find Possible Source Effects** ✅ landed above. **Convert To Effect** remains the only render-down gap — deferred this pass: it needs a render-to-effect bridge over the core render-down path, larger than the menu adds.)
- **User-customizable keybindings + hotkey-to-apply-effect/preset/setting** — Desktop `KeyBindingEditDialog` + `KeyBindings` (MainSequencer.cpp:628-664). iPad shortcuts are hardcoded in `XLightsCommands.swift`. A keybinding editor on iPad is large and low-value vs the AC work; the hotkey-to-apply-effect path is feasible but niche.
- **Paste As Layers mode (+ Alternate Paste)** — iPad `pasteAtRow` auto-inserts layers (SequencerViewModel.swift:5991) but exposes no explicit Paste-As-Layers / Alternate-Paste mode (no `PasteAsLayers`/`ALTERNATE_PASTE`/`alternatePaste` refs in src-iPad). Work: add an explicit paste-as-layers/alternate-paste option over the existing core paste path.

## Desktop gaps (iPad has, desktop missing)

### P2
- **Select All in Column (true span-select across all rows)** — iPad `selectAllEffectsInColumn` (VM:4401, SequencerGridV2View.swift:644) selects every effect overlapping a time span across all model rows. Desktop's closest is the AC cell-range, which is a different selection model. Could be added to desktop's grid context menu over the same overlap query. Ease easy.

### P3
- **Per-mark Split / Merge timing-mark context items** — iPad exposes `splitTimingMark` (VM:3634) and `mergeTimingMarkWithNext` (VM:3662) as per-mark long-press items. Desktop has a `TIMING_SPLIT` keybinding but no right-click "Split/Merge this mark" entry. Adding to the desktop timing context menu would be a small parity add. Ease medium.
- **Transition-type picker from the fade handle** — iPad long-press on a fade handle picks Fade/Wipe/etc. inline (TransitionPickerDialog). Desktop only sets transition type via the Blending panel combos. A desktop right-click-on-fade-handle shortcut would mirror it. Ease medium.

## Infeasible / restricted on iPad

- **Two-finger marquee, Pencil double-tap/squeeze→undo, pointer-hover handle highlights** — iPad-only touch/Pencil idioms with no desktop hardware analog; desktop already covers the same intent with mouse drag-rect and cursor feedback. Not gaps to "fix."
- **No controller-firmware items in this theme** — Sequencer grid editing carries no controller-config/upload surface, so the open-source-firmware restriction does not apply here.
- **AC toolbar is feasibility=hard, not infeasible** — it is a UI-architecture investment, not a platform limitation; the underlying AC render ops are wx-free core and reusable. **Deferred (P3) by decision 2026-06-11**; see the Deferred section above for the touch-first rescoping guidance.

## Recommended sequencing

1. **Cheap parity wins (P2/P3, easy)** — ✅ mostly **landed**: Cut, Play-Model + Play-Effect, Move-effect-up/down-a-layer, Paste-Mode toggle, Enable-Render-on-All-Models, Delete-Multiple-Layers, **Copy-incl-SubModels**, **Find-Possible-Source-Effects**, **Paste-at-Original-Time**, and **per-region `.xsq` export**. Still open from this batch: scoped SubModel/Strand/Node deletes and Convert-To-Effect (the render-down, larger than a menu add).
2. **Select All in Column on desktop** (the one easy reverse-parity item) — small grid-context-menu add.
3. **AC toolbar — deferred (2026-06-11).** Do not build unless user demand surfaces; if it does, design touch-first over the existing span-select rather than porting the desktop cell-cursor (see Deferred section).
4. **Defer indefinitely:** user-customizable keybinding editor and hotkey-to-apply-effect (P3, hard, low value).
