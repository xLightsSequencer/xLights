# Staging area only. ci_scripts/fetch_dependencies.ps1 unpacks the dependency
# bundle and KLightMapper here at build time; nothing in this directory belongs
# in the repository. Ignore everything so binaries cannot drift back in.
*
!.gitignore
